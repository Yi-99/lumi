[project]
name = "lumi-server"
version = "0.1.0"
description = "Local lookup proxy for the lumi extension — SSE fan-out to LLM providers"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "slowapi>=0.1.9",
]
